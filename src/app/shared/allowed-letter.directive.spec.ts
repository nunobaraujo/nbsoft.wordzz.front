import { AllowedLetterDirective } from './allowed-letter.directive';

describe('AllowedLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedLetterDirective();
    expect(directive).toBeTruthy();
  });
});
